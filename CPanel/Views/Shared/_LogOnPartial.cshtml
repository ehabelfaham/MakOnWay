@if (Request.IsAuthenticated && Session["UserId"]!=null)
{
    Session["UserName"] = User.Identity.Name;
    Session["EmpRole"] = CPanel.Controllers.MyAccountController.SetEmpRole(User.Identity.Name);
    //if (Session["Emp_id"] == null)
    //{
    //    Response.Redirect("~/Account/LogOn");
    //}
    <text>
    <div class="floatleft">
    <img src="@Url.Content("~/Content/img/logo.png")" alt="Logo" /></div>
                <div class="floatright">
                    <div class="floatleft">
                        <img src="@Url.Content("~/Content/img/img-profile.jpg")" alt="Profile Pic" /></div>
                    <div class="floatleft marginleft10">
                        <ul class="inline-ul floatleft">
                            <li>@User.Identity.Name</li>
                            <li>@Html.ActionLink("Config", "ChangePassword", "MyAccount")</li>
                            <li>@Html.ActionLink("Log Off", "LogOff", "MyAccount")</li>
                        </ul>
                        <br />
                        <span class="small grey">Welcome</span>
                    </div>
                </div>
   </text>
    }else {
     Response.Redirect("~/MyAccount/LogOn");
    }
